name: Generate Manifest

on:
  push:
    branches:
      - main
  workflow_dispatch: # Allows manual triggering from the Actions tab

jobs:
  generate-manifest:
    runs-on: ubuntu-latest

    steps:
      # Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # Set up Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # Run the script to generate the manifest.json
      # Shoutout to https://github.com/orgs/community/discussions/26248#discussioncomment-4766974
      # for finding the correct way to run the script wow this was hard to find
      - name: Run manifest generation script
        run: node ${{ github.action_path }}/generateManifest.js
        shell: bash

      # Check if the manifest.json has changed
      - name: Check for changes
        run: |
          git diff --exit-code ./manifest.json || echo "Changes detected"

      # Stage and commit the changes if any were made
      - name: Commit and push changes
        if: ${{ success() && (steps.Check_for_changes.outputs.changed == 'true') }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add ./manifest.json
          git commit -m "Update manifest.json"
          git push
